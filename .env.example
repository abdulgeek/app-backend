# Environment Variables Template

# MongoDB Atlas Connection
# Replace <username>, <password>, and <cluster-url> with your actual MongoDB Atlas credentials
# Example: mongodb+srv://myuser:mypassword@cluster0.xxxxx.mongodb.net/todoapp?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/todoapp?retryWrites=true&w=majority

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# CORS Origin (Frontend URL)
# For local development
CORS_ORIGIN=http://localhost:3000
# For production (Vercel deployment)
# CORS_ORIGIN=https://your-app.vercel.app

# API Base Path (optional)
API_BASE_PATH=/api

# MongoDB Options (optional)
DB_NAME=todoapp

# Rate Limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100